{
  "name": "streamhub-assessment",
  "version": "1.0.0",
  "scripts": {
    "test:ui": "cucumber-js features/ --require step-definitions/ --require config/",
    "test:api": "node tests/api/api.test.js",
    "test": "npm run test:ui && npm run test:api"
  },
  "dependencies": {
    "@cucumber/cucumber": "^10.0.0",
    "@playwright/test": "^1.40.0",
    "playwright": "^1.40.0"
  },
  "devDependencies": {}
}
